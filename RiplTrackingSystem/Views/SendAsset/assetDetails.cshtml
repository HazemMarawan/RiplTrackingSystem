
@{
    ViewBag.Title = "assetDetails";
    Layout = "~/Views/Shared/_Layout.cshtml";

}


<div class="modal fade" id="manage_sendAsset_modal" tabindex="-1" role="dialog" aria-hidden="true">
    <form action="" method="post" enctype='multipart/form-data'>
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal_title"></h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="form-body">
                        <div class="form-row">
                            <input type="hidden" name="id" id="id" value="0" />
                            <div class="form-group col-md-12">
                                <label>Location</label>
                                @Html.DropDownList("location_id", new SelectList(ViewBag.locations, "id", "name"), "Select Location", new { @class = "selectize", @name = "location_id", @id = "location_id", @required = "required" })
                            </div>
                            <div class="form-group col-md-12">
                                <label>Asset</label>
                                @Html.DropDownList("assets_ids", new SelectList(ViewBag.assets, "id", "tag_id"), "Select Asset", new { @class = "selectize", @name = "assets_ids", @id = "asset_id", @required = "required", @multiple = "multiple" })
                            </div>
                            <div class="form-group col-md-6">
                                <label>Start Date</label>
                                <input type="date" name="start_date" id="start_date" class="form-control radius-30" />
                            </div>
                            <div class="form-group col-md-6">
                                <label>Due Date</label>
                                <input type="date" name="due_date" id="due_date" class="form-control radius-30" />
                            </div>
                            @*<div class="form-group col-md-12">
                                    <label>Permission Group</label>
                                    @Html.DropDownList("permission_group_id", new SelectList(ViewBag.locations, "id", "name"), "Select Group", new { @class = "form-control", @name = "permission_group_id", @id = "permission_group_id", @required = "required" })
                                </div>*@

                        </div>


                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </form>
</div>

<!--breadcrumb-->
<div class="page-breadcrumb d-none d-md-flex align-items-center mb-3">
    <div class="breadcrumb-title pr-3">Receive Asset</div>
    <div class="pl-3">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0 p-0">
                <li class="breadcrumb-item">
                    <a href="@Url.Action("Index","Dashboard")"><i class='bx bx-home-alt'></i></a>
                </li>
                <li class="breadcrumb-item">
                    <a href="@Url.Action("Index","SendAsset")">Send Assets</a>
                </li>
                <li class="breadcrumb-item active" aria-current="page">Receive Assets</li>
            </ol>
        </nav>
    </div>
    @*<div class="ml-auto">
        <div class="btn-group">
            <button type="button" class="btn btn-primary">Settings</button>
            <button type="button" class="btn btn-primary bg-split-primary dropdown-toggle dropdown-toggle-split" data-toggle="dropdown">
                <span class="sr-only">Toggle Dropdown</span>
            </button>
            <div class="dropdown-menu dropdown-menu-right dropdown-menu-lg-left">
                <a class="dropdown-item" href="javascript:;" id="add_sendAsset">Send Asset</a>
            </div>
        </div>
    </div>*@
</div>
<!--end breadcrumb-->

<div class="card radius-15">
    <div class="card-body">
        <div class="row">
            <div class="form-group col-4">
                <input class="form-control" type="text" id="text_search" name="text_search" placeholder="Search">
            </div>
            @*<div class="form-group col-2">
                    <input class="result form-control" type="text" id="date-form" placeholder="Date Picker...">
                </div>
                <div class="form-group col-2">
                    <input class="result form-control" type="text" id="date-to" placeholder="Date Picker...">
                </div>*@

            <div class="form-group col-2">
                <button id="search" class="btn btn-primary px-3 radius-30">Search</button>
            </div>
            <div class="form-group col-2">
                <button id="reset" class="btn btn-danger px-3 radius-30">Reset</button>
            </div>

        </div>
    </div>
</div>
<div class="card">
    <div class="card-body">
        <input type="hidden" id="rentOrder" value="@ViewBag.id" />
        <div class="table-responsive">
            <table id="sendAssetTable" class="table table-striped table-bordered" style="width:100%">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Location</th>
                        <th>Type</th>
                        <th># of Assets</th>
                        <th>Start Date</th>
                        <th>Due Date</th>
                        <th>Assets</th>
                        <th></th>
                    </tr>
                </thead>

            </table>
        </div>
    </div>
</div>

@section scripts
{
    <script>
       $(document).ready(function () {
           var id = $("#rentOrder").val();
           var datatable = $('#sendAssetTable')
                .DataTable({
                    "processing": true,
                    "serverSide": true,
                    "scrollX": true,
                    "stateSave": false,
                    "lengthChange": false,
                    "dom": 'Bfrtip',

                    "buttons": ['copy', 'excel', 'pdf', 'print'],
                    "pageLength":10,
                    "rowId": 'id',
                    "ajax": {
                        "url": "/SendAsset/assetDetails/" + id,
                        "type": "POST",
                        "datatype": "json",
                        "dataSrc": "data"
                    },
                    "columns": [
                        {
                            "data": "id",
                            "searchable": true
                        },
                        {
                            "data": "location_name",
                            "searchable": true
                        },
                        {
                            "data": "location_type",
                            "searchable": true
                        },
                        {
                            "data": "assets_count",
                            "searchable": true
                        },
                        {
                            "data": "start_date", "searchable": true,
                            fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                                var num = parseInt(oData.start_date.replace(/[^0-9]/g, ""));
                                var date = new Date(num);

                                var html = date.toDateString();
                                $(nTd).html("<span class='action-column'>" + html + "</span>");
                            }
                        },
                        {
                            "data": "due_date", "searchable": true,
                            fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                                var num = parseInt(oData.due_date.replace(/[^0-9]/g, ""));
                                var date = new Date(num);

                                var html = date.toDateString();
                                $(nTd).html("<span class='action-column'>" + html + "</span>");
                            }
                        },
                        {
                            "data": "assetsStatus", "searchable": true,
                            fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                                var html = "";
                                oData.assetsStatus.forEach(function (value) {
                                    if (value.status == 1)
                                        html += "<span class='badge badgeUrlinfo'>" + value.tag_id + " => Wating For Receive</span>&nbsp;";
                                    else if (value.status == 2)
                                        html += "<span class='badge badge-info'>" + value.tag_id + " => Received </span>&nbsp;";
                                    else if (value.status == 3)
                                        html += "<span class='badge badge-info'>" + value.tag_id + " => Lost </span>&nbsp;";

                                });
                                $(nTd).html("<span class='action-column'>" + html + "</span>");
                            }
                        },
                        {
                            "data": "id", "searchable": true,
                            fnCreatedCell: function (nTd, sData, oData, iRow, iCol) {
                                var html = "";

                                html += "<a class='detail' id='" + oData.id + "' href='/SendAsset/assetDetails/" + oData.id + "' title='Asset Details'><i class='lni lni-eye'></i></a>&nbsp;";
                                html += "<a class='edit' id='" + oData.id + "' href='javascript:void(0)' title='Edit'><i class='lni lni-pencil'></i></a>&nbsp;";
                                html += "<a class='delete' id='" + oData.id + "' href='javascript:void(0)' title='Delete'><i class='lni lni-trash'></i></a>";

                                $(nTd).html("<span class='action-column'>" + html + "</span>");
                            }
                        },
                    ]
                });

            $('#search').on('click', function () {
                datatable.search($("#text_search").val());
                //datatable.columns(0).search($("#date-form").val());
                //datatable.columns(1).search($("#date-to").val());
                datatable.draw();
            });

            $('#text_search').on('keyup', function (e) {
                if (e.keyCode == 13)
                    $('#search_button').trigger('click');
            });

            $('#reset').on('click', function () {
                $("#text_search").val("");

                $('#search').trigger('click');

            });

            //$('#date-form').bootstrapMaterialDatePicker({
            //    time: false
            //});

            //$('#date-to').bootstrapMaterialDatePicker({
            //    time: false
            //});

           $("#add_sendAsset").click(function () {
               var modal = $('#manage_sendAsset_modal').clone();
                var action = '@Url.Action("saveSendAsset", "SendAsset")';
                modal.find('form').attr('action', action);
                modal.find('#modal_title').text('Send Asset');
                modal.execModal({
                    progressBar: 'progress_bar',
                    progressText: 'progress_text',
                }, function (response) {

                        datatable.draw();

                }, function (response) {

                }, function (dialog) {
                        dialog.find('#location_id').selectize({
                            plugins: ['remove_button'],
                        });
                        dialog.find('#asset_id').selectize({
                            plugins: ['remove_button'],
                        });
                });
            });

            $(document).on("click", ".edit", function () {
                var data = datatable.row($(this).closest('tr')).data();
                console.log(data);
                var modal = $('#manage_sendAsset_modal').clone();
                var action = '@Url.Action("saveSendAsset", "SendAsset")';
                modal.find('form').attr('action', action);
                modal.find('#modal_title').text('Edit Rented Asset');
                modal.execModal({
                    progressBar: 'progress_bar',
                    progressText: 'progress_text',
                }, function (response) {
                        datatable.draw();

                }, function (response) {

                }, function (dialog) {
                        dialog.find('#id').val(data.id);
                        var value = new Date
                            (
                                parseInt(data.start_date.replace(/(^.*\()|([+-].*$)/g, ''))
                            );
                        var date_start = value.getFullYear() +
                            "-" +
                            ("0" + (value.getMonth() + 1)).slice(-2) +
                            "-" +
                            ("0" + value.getDate()).slice(-2)
                            ;
                        dialog.find('#start_date').val(date_start);
                        var value = new Date
                            (
                                parseInt(data.due_date.replace(/(^.*\()|([+-].*$)/g, ''))
                            );
                        var date_due = value.getFullYear() +
                            "-" +
                            ("0" + (value.getMonth() + 1)).slice(-2) +
                            "-" +
                            ("0" + value.getDate()).slice(-2)
                            ;
                        dialog.find('#due_date').val(date_due);
                        dialog.find('#location_id option[value=' + data.location_id + ']').attr('selected', 'selected');
                        var ids = [];
                        var $select = dialog.find('#asset_id').selectize({
                            plugins: ['remove_button'],
                        });
                        $select[0].selectize.clear();
                        var selectize = $select[0].selectize;
                        console.log(data.assets_ids);
                        data.assets_ids.forEach(function (value) {
                            ids.push(value);

                        });
                        selectize.setValue(ids);
                        dialog.find('#location_id').selectize({
                            plugins: ['remove_button'],
                        });

                });

            });

            $(document).on("click", ".delete", function () {
                var data = datatable.row($(this).closest('tr')).data();
                warningBox("Are you sure you want to delete this Sent Assetes ?", function () {
                    $.ajax({
                        type: "GET",
                        url: '@Url.Action("deleteSendAsset", "SendAsset")',
                        contentType: "application/json; charset=utf-8",
                        data: { "id": data.id },
                        datatype: "json",
                        success: function (data) {
                            datatable.draw();
                        },
                        error: function () {
                            alert("Dynamic content load failed.");
                        }
                    });
                });
            });

        });

    </script>
}

