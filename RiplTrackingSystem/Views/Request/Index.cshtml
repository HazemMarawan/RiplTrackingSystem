
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-header">
    <nav class="breadcrumb-one" aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="javascript:void(0);">Requests</a></li>
        </ol>
    </nav>
    <div class="dropdown filter custom-dropdown-icon">
        <a class="dropdown-toggle btn" href="#" role="button" id="filterDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><span class="text">More</span> <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-chevron-down"><polyline points="6 9 12 15 18 9"></polyline></svg></a>

        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="filterDropdown">
            <a class="dropdown-item" id="export_requests_report" href="javascript:void(0);" data-value="<span>Show</span> : Export Available Assets Report">Export Requests Report</a>
        </div>
    </div>
</div>


<form action="" id="filter_form" method="post">
    <div class="row">
        <div class="form-group col-3">
            <input class="form-control" type="text" id="text_search" name="text_search" placeholder="Search">
        </div>
        <div class="form-group col-3">
            <select name="request_type" id="request_type" class="form-control">
                <option value="">Select Type</option>
                <option value="1">New</option>
                <option value="2">Return</option>
            </select>
        </div>
        <div class="form-group col-md-3">
            @Html.DropDownList("location_id", new SelectList(ViewBag.locations, "id", "name"), "Select Location", new { @class = "form-control", @name = "location_id", @id = "location_id" })
        </div>

        <div class="form-group col-3">
            <input class="form-control" type="date" id="from_date" name="from_date">
        </div>
        <div class="form-group col-3">
            <input class="form-control" type="date" id="to_date" name="to_date">
        </div>

        <div class="form-group">
            <button id="search" type="button" class="btn btn-primary px-3 radius-30">Search</button>
        </div>
        <div class="form-group col-3">
            <button id="reset" type="reset" class="btn btn-danger px-3 radius-30">Reset</button>
        </div>

    </div>
</form>


<div class="card">
    <div class="card-body">
        <table id="requestsTable" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Location</th>
                    <th>Number Of Assets</th>
                    <th>Notes</th>
                    <th>Created By</th>
                    <th>Type</th>
                    <th>Created At</th>

                </tr>
            </thead>

        </table>
    </div>
</div>
@section scripts
{
    <script>
       $(document).ready(function () {

           var datatable = $('#requestsTable')
                .DataTable({
                    "processing": true,
                    "serverSide": true,
                    "scrollX": true,
                    "stateSave": false,
                    "lengthChange": false,
                    "dom": 'Bfrtip',

                    "buttons": ['copy', 'excel', 'pdf', 'print'],
                    "pageLength":10,
                    "rowId": 'id',
                    "ajax": {
                        "url": "@Url.Action("Index", "Request")",
                        "type": "POST",
                        "datatype": "json",
                        "dataSrc": "data"
                    },
                    "columns": [
                        {
                            "data": "id",
                            "searchable": true
                        },
                        {
                            "data": "stringLocation",
                            "searchable": true
                        },
                        {
                            "data": "number_of_assets",
                            "searchable": true
                        },
                        {
                            "data": "notes",
                            "searchable": true
                        },
                        {
                            "data": "stringUser",
                            "searchable": true
                        },
                        {
                            "data": "stringType",
                            "searchable": true
                        },
                        {
                            "data": "stringCreated_at",
                            "searchable": true
                        }

                    ]
                });

            $('#search').on('click', function () {
                datatable.search($("#text_search").val());
                datatable.columns(0).search($("#request_type").val());
                datatable.columns(1).search($("#location_id").val());
                datatable.columns(2).search($("#from_date").val());
                datatable.columns(3).search($("#to_date").val());

                datatable.draw();
            });

            $('#text_search').on('keyup', function (e) {
                if (e.keyCode == 13)
                    $('#search').trigger('click');
            });

            $('#reset').on('click', function () {
                $("#text_search").val("");

                $('#search').trigger('click');

            });
           $(document).on("click", "#export_requests_report", function () {
               var action = '@Url.Action("RequestsReport", "Report")';
               $('#filter_form').attr('action', action);
               $('#filter_form').submit();
           });

        });

    </script>
}

